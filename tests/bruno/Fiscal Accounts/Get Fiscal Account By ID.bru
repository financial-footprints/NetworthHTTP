meta {
  name: Get Fiscal Account By ID
  type: http
  seq: 5
}

get {
  url: {{BASEURL}}/api/v1/fiscal_accounts/{{accountId}}
  body: none
  auth: none
}

vars:pre-request {
  accountId: {{REQ_ACCOUNT_ID}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{REQ_ACCOUNT_ID}}
}
